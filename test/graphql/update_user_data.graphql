mutation UpdateUserData(
  $profileItems: [UserProfileItem!]!
  $userId: ID!
  $location: Location!
) {
  updateUser(
    profileItems: $profileItems
    userId: $userId
    location: $location
  ) {
    __typename
  }
}
